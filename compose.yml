networks:
    dev:

services:
  nginx:
    image: nginx
    container_name: server
    ports:
      - 8000:80
    networks:
      - dev
  
  api:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
    - 80:80
    networks:
      - dev  
    depends_on:
      - db
    
  db:
    image: postgres
    container_name: db
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: db
    networks:
      - dev